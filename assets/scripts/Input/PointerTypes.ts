export interface PointerWorldPosition {
    x: number;
    y: number;
}

export interface BoardProjectionConfig {
    originWorldX: number;
    originWorldY: number;
    cellSize: number;
    gridWidth: number;
    gridHeight: number;
}
